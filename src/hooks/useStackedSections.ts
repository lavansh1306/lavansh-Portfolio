import { useEffect } from 'react';

export const useStackedSections = () => {
  useEffect(() => {
    // Disabled - GSAP ScrollTrigger conflicts with Lenis smooth scroll
    // Implement stacked sections with Framer Motion whileInView instead
    return;
  }, []);
};
