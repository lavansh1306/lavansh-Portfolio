name: Build and Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  # allow manual dispatch from Actions UI for debugging or force runs
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # ensure full history is fetched and credentials are available for pushing
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          # allow creating an empty commit so the gh-pages branch/commit updates even
          # when build output didn't change — useful during iterative debugging
          allow_empty_commit: true
